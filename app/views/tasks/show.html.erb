<div class="d-flex">
  <h1><%= @task.title %></h1>
  <%= link_to "edit this task", edit_path, class: 'align-self-end btn btn-primary' %>
</div>
<div class="alert alert-secondary">
  <p><%= @task.details %></p>
</div>

<input type="checkbox"> <%= @task.completed == false ? "Not completed" : "Completed" %>

<%= link_to "back to all tasks", tasks_path %></p>
